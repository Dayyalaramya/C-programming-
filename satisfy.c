#include <stdio.h>

int main() {
    int num;
    printf("enter the number:");
    scanf("%d",&num);
    if(num>=10)
    {
    printf("satisfy the condition");
    }
    else
    {
    printf("not satisfy the condition");
    }

    return 0;
}